%p.newlink= link_to('New Genre', new_genre_path, :class => 'button radius') if @auth.present? && @auth.is_admin
%table
  - @genres.each do |genre|
    %tr
      %td= genre.name
      %td= link_to('Edit', edit_genre_path(genre), :class => 'button radius tiny') if @auth.present? && @auth.is_admin
